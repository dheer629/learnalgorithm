apiVersion: v1
kind: Service
metadata:
  name: learnalgorithm-frontend
  namespace: learnalgorithm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: learnalgorithm-frontend
